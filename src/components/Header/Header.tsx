import {FC} from 'react';
import {Container} from '@mui/material';

const Header: FC = () => {
    return (
        <header>
            <Container>
                HEADER
            </Container>
        </header>
    )
}

export default Header;
